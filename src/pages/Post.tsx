import type { FC } from 'react';

const Post: FC<unknown> = () => {
	return (
		<>
			<p>This page will have forms for sending webhook messages</p>
			<p>This will have a send button</p>
		</>
	);
};

export default Post;
